# Sound It

Start with `PROJECT_CONTEXT.md`.

This repo contains the Sound It backend and web app. The main project overview, current status, completed fixes, and next-step guidance now live in `PROJECT_CONTEXT.md` so work can resume without duplicating previous fixes.
